on: push


jobs:
  anythings:
    run-on: ubuntu-latest
  env:                                                                            
    API_KEY: ${{ secrets.API_KEY }}  
  uses: actions/checkout@v4
  steps:
    run: echo "Key length: ${#API_KEY}"


